class ConstantUtils {
  static const defaultPadding = 20.0;
  static const defaultDuration = Duration(seconds: 1);
  static const speedTyperAnimatedText = Duration(milliseconds: 60);
  static const maxWidth = 1440.0;
}
